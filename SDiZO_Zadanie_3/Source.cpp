#include "TSM.h"

int main()
{
	/*
	vector<int> vec;

	vec.push_back(4);
	vec.push_back(3);
	vec.push_back(2);
	vec.push_back(1);

	sort(vec.begin(), vec.end());

	do 
	{
		cout << vec[0] << " " << vec[1] << " " << vec[2] << " " << vec[3] << " \n";
	} while (next_permutation(vec.begin()+1, vec.end()));
	cout << vec[0] << " " << vec[1] << " " << vec[2] << " " << vec[3] << " ";
	*/

	TSM tsm;

	system("PAUSE");
	return 0;
}